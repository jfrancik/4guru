<%= form_for(@screening) do |f| %>
  <% if @screening.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@screening.errors.count, "error") %> prohibited this screening from being saved:</h2>

      <ul>
      <% @screening.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cinema_id %><br />
    <%= f.select :cinema_id, Cinema.all.collect {|cinema| [cinema.name, cinema.id]} %>
  </div>
  <div class="field">
    <%= f.label :film_id %><br />
    <%= f.select :film_id, Film.all.collect {|film| [film.title, film.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
